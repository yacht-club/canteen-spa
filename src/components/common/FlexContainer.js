import styled from 'styled-components';

const FlexContainer = styled.div`
  margin-bottom: 24px;
  display: flex;
  display: -webkit-flex;
  flex-wrap: wrap;
  flex-direction: row;
  align-items: stretch;
`;

export default FlexContainer;

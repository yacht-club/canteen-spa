import styled from 'styled-components';

const FlexContainer = styled.div`
  display: flex;
  display: -webkit-flex;
  flex-wrap: wrap;
  flex-direction: row;
  align-items: stretch;
`;

export default FlexContainer;
